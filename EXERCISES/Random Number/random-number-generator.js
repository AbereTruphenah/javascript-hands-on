//let randomNum=Math.random();// returns a random number between 0 and 1.
//let randomNum=Math.floor(Math.random());//rounds down the generated random number.
//let randomNum=Math.floor(Math.random()*5);//random number between 0 and 5; 5 excluded.
let randomNum=Math.floor(Math.random()*5)+1;//increases the generated random number by 1
console.log(randomNum);